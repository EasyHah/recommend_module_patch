/node_modules

# Build output (Vite/webpack)
/dist

# Large media in public (do not commit)
/public/FireEvacuation/*.mp4

# Local environment files (do not commit secrets)
# ignore all .env.* except the example
.env*
!.env.example